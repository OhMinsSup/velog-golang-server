// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"github.com/OhMinsSup/story-server/ent/user"
	"github.com/OhMinsSup/story-server/ent/usermeta"
	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
	"github.com/google/uuid"
)

// UserMetaCreate is the builder for creating a UserMeta entity.
type UserMetaCreate struct {
	config
	mutation *UserMetaMutation
	hooks    []Hook
}

// SetEmailNotification sets the "email_notification" field.
func (umc *UserMetaCreate) SetEmailNotification(b bool) *UserMetaCreate {
	umc.mutation.SetEmailNotification(b)
	return umc
}

// SetNillableEmailNotification sets the "email_notification" field if the given value is not nil.
func (umc *UserMetaCreate) SetNillableEmailNotification(b *bool) *UserMetaCreate {
	if b != nil {
		umc.SetEmailNotification(*b)
	}
	return umc
}

// SetEmailPromotions sets the "email_promotions" field.
func (umc *UserMetaCreate) SetEmailPromotions(b bool) *UserMetaCreate {
	umc.mutation.SetEmailPromotions(b)
	return umc
}

// SetNillableEmailPromotions sets the "email_promotions" field if the given value is not nil.
func (umc *UserMetaCreate) SetNillableEmailPromotions(b *bool) *UserMetaCreate {
	if b != nil {
		umc.SetEmailPromotions(*b)
	}
	return umc
}

// SetCreatedAt sets the "created_at" field.
func (umc *UserMetaCreate) SetCreatedAt(t time.Time) *UserMetaCreate {
	umc.mutation.SetCreatedAt(t)
	return umc
}

// SetNillableCreatedAt sets the "created_at" field if the given value is not nil.
func (umc *UserMetaCreate) SetNillableCreatedAt(t *time.Time) *UserMetaCreate {
	if t != nil {
		umc.SetCreatedAt(*t)
	}
	return umc
}

// SetUpdatedAt sets the "updated_at" field.
func (umc *UserMetaCreate) SetUpdatedAt(t time.Time) *UserMetaCreate {
	umc.mutation.SetUpdatedAt(t)
	return umc
}

// SetNillableUpdatedAt sets the "updated_at" field if the given value is not nil.
func (umc *UserMetaCreate) SetNillableUpdatedAt(t *time.Time) *UserMetaCreate {
	if t != nil {
		umc.SetUpdatedAt(*t)
	}
	return umc
}

// SetID sets the "id" field.
func (umc *UserMetaCreate) SetID(u uuid.UUID) *UserMetaCreate {
	umc.mutation.SetID(u)
	return umc
}

// SetUserID sets the "user" edge to the User entity by ID.
func (umc *UserMetaCreate) SetUserID(id uuid.UUID) *UserMetaCreate {
	umc.mutation.SetUserID(id)
	return umc
}

// SetUser sets the "user" edge to the User entity.
func (umc *UserMetaCreate) SetUser(u *User) *UserMetaCreate {
	return umc.SetUserID(u.ID)
}

// Mutation returns the UserMetaMutation object of the builder.
func (umc *UserMetaCreate) Mutation() *UserMetaMutation {
	return umc.mutation
}

// Save creates the UserMeta in the database.
func (umc *UserMetaCreate) Save(ctx context.Context) (*UserMeta, error) {
	var (
		err  error
		node *UserMeta
	)
	umc.defaults()
	if len(umc.hooks) == 0 {
		if err = umc.check(); err != nil {
			return nil, err
		}
		node, err = umc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*UserMetaMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = umc.check(); err != nil {
				return nil, err
			}
			umc.mutation = mutation
			node, err = umc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(umc.hooks) - 1; i >= 0; i-- {
			mut = umc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, umc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (umc *UserMetaCreate) SaveX(ctx context.Context) *UserMeta {
	v, err := umc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// defaults sets the default values of the builder before save.
func (umc *UserMetaCreate) defaults() {
	if _, ok := umc.mutation.EmailNotification(); !ok {
		v := usermeta.DefaultEmailNotification
		umc.mutation.SetEmailNotification(v)
	}
	if _, ok := umc.mutation.EmailPromotions(); !ok {
		v := usermeta.DefaultEmailPromotions
		umc.mutation.SetEmailPromotions(v)
	}
	if _, ok := umc.mutation.CreatedAt(); !ok {
		v := usermeta.DefaultCreatedAt()
		umc.mutation.SetCreatedAt(v)
	}
	if _, ok := umc.mutation.UpdatedAt(); !ok {
		v := usermeta.DefaultUpdatedAt()
		umc.mutation.SetUpdatedAt(v)
	}
	if _, ok := umc.mutation.ID(); !ok {
		v := usermeta.DefaultID()
		umc.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (umc *UserMetaCreate) check() error {
	if _, ok := umc.mutation.EmailNotification(); !ok {
		return &ValidationError{Name: "email_notification", err: errors.New("ent: missing required field \"email_notification\"")}
	}
	if _, ok := umc.mutation.EmailPromotions(); !ok {
		return &ValidationError{Name: "email_promotions", err: errors.New("ent: missing required field \"email_promotions\"")}
	}
	if _, ok := umc.mutation.CreatedAt(); !ok {
		return &ValidationError{Name: "created_at", err: errors.New("ent: missing required field \"created_at\"")}
	}
	if _, ok := umc.mutation.UpdatedAt(); !ok {
		return &ValidationError{Name: "updated_at", err: errors.New("ent: missing required field \"updated_at\"")}
	}
	if _, ok := umc.mutation.UserID(); !ok {
		return &ValidationError{Name: "user", err: errors.New("ent: missing required edge \"user\"")}
	}
	return nil
}

func (umc *UserMetaCreate) sqlSave(ctx context.Context) (*UserMeta, error) {
	_node, _spec := umc.createSpec()
	if err := sqlgraph.CreateNode(ctx, umc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	return _node, nil
}

func (umc *UserMetaCreate) createSpec() (*UserMeta, *sqlgraph.CreateSpec) {
	var (
		_node = &UserMeta{config: umc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: usermeta.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: usermeta.FieldID,
			},
		}
	)
	if id, ok := umc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := umc.mutation.EmailNotification(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeBool,
			Value:  value,
			Column: usermeta.FieldEmailNotification,
		})
		_node.EmailNotification = value
	}
	if value, ok := umc.mutation.EmailPromotions(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeBool,
			Value:  value,
			Column: usermeta.FieldEmailPromotions,
		})
		_node.EmailPromotions = value
	}
	if value, ok := umc.mutation.CreatedAt(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: usermeta.FieldCreatedAt,
		})
		_node.CreatedAt = value
	}
	if value, ok := umc.mutation.UpdatedAt(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: usermeta.FieldUpdatedAt,
		})
		_node.UpdatedAt = value
	}
	if nodes := umc.mutation.UserIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2O,
			Inverse: true,
			Table:   usermeta.UserTable,
			Columns: []string{usermeta.UserColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeUUID,
					Column: user.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// UserMetaCreateBulk is the builder for creating many UserMeta entities in bulk.
type UserMetaCreateBulk struct {
	config
	builders []*UserMetaCreate
}

// Save creates the UserMeta entities in the database.
func (umcb *UserMetaCreateBulk) Save(ctx context.Context) ([]*UserMeta, error) {
	specs := make([]*sqlgraph.CreateSpec, len(umcb.builders))
	nodes := make([]*UserMeta, len(umcb.builders))
	mutators := make([]Mutator, len(umcb.builders))
	for i := range umcb.builders {
		func(i int, root context.Context) {
			builder := umcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*UserMetaMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, umcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, umcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, umcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (umcb *UserMetaCreateBulk) SaveX(ctx context.Context) []*UserMeta {
	v, err := umcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
